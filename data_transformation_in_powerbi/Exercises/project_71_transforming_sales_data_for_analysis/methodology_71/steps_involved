1. In Power Query, start by grouping the sales data by Employee Name, Year and Month
2. Pivot the month column, using the Total Sales Amount as the value column
3. Filter the Name column so that only Steven Buchanan's sales are in the table
